filebeat.config:
  modules:
    path: ${path.config}/modules.d/*.yml
    reload.enabled: false

processors:
  - add_cloud_metadata: ~
  - add_docker_metadata: ~

output.elasticsearch:
  hosts: 'elasticsearch:9200'

http.enabled: true
#http.port: 5066
monitoring.enabled: false
#monitoring.cluster_uuid: "Lew0js7ARa6Mircl9IfXbQ"
